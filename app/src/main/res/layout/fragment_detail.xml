<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <!-- 구분선 -->
    <View
        android:id="@+id/nav_underbar"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_margin="20dp"
        android:background="#999999"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 상세 기록 -->
    <LinearLayout
        android:id="@+id/detail_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/nav_underbar"
        app:layout_constraintBottom_toBottomOf="parent"
        android:orientation="vertical"
        android:layout_margin="10dp">

        <!-- 상세 기록 이름 -->
        <TextView
            android:id="@+id/detail_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="20sp"
            android:text="상세 기록"
            android:layout_margin="10dp" />

        <!-- 구분선 -->
        <View
            android:id="@+id/detail_underbar"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#999999" />

        <!-- 상세 기록 목록 -->
        <LinearLayout
            android:id="@+id/date_detail_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_margin="10dp">

            <!-- 날짜별 기록을 위한 클릭 가능한 영역 (사진을 찍은 날이 있으면 추가) -->
            <LinearLayout
                android:id="@+id/btn_date_detail"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:layout_margin="10dp"
                android:orientation="vertical"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground">

                <!-- 촬영 날짜 (실제 촬영한 날짜가 출력해야 함) -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="0000년 00월 00일"
                    android:textSize="20sp"
                    android:textStyle="bold" />

                <!-- 사진 -->
                <ScrollView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content">

                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal">

                        <!-- 사진 (찍을 때마다 추가해야 함) -->
                        <ImageButton
                            android:layout_width="80dp"
                            android:layout_height="100dp"
                            android:contentDescription="사진" />

                        <ImageButton
                            android:layout_width="80dp"
                            android:layout_height="100dp"
                            android:contentDescription="사진" />
                    </LinearLayout>
                </ScrollView>
            </LinearLayout>

            <!-- 예시로 하나 더 작성함 -->
            <LinearLayout
                android:id="@+id/btn_date_detail02"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:layout_margin="10dp"
                android:orientation="vertical"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground">

                <!-- 촬영 날짜 -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="0000년 00월 00일"
                    android:textSize="20sp"
                    android:textStyle="bold" />

                <!-- 사진 -->
                <ScrollView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content">

                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal">

                        <!-- 사진 (찍을 때마다 추가해야 함) -->
                        <ImageButton
                            android:layout_width="80dp"
                            android:layout_height="100dp"
                            android:contentDescription="사진" />

                        <ImageButton
                            android:layout_width="80dp"
                            android:layout_height="100dp"
                            android:contentDescription="사진" />
                    </LinearLayout>
                </ScrollView>
            </LinearLayout>

        </LinearLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
